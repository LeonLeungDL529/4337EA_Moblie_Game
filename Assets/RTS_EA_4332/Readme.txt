 Created by VTC Stu
 For the Unity Asset Store 
 This asset falls under the " License"   
 For support email leonleung529@gmail.com'


	Essay AI for RTS && Basic UI
Easy AI and Basic movement of the RTS which player can use mouse to control
basic shooting might working

   /*if (Physics.Raycast(transform.position, transform.forward, out H,  150, ~1 << 8))
            //if (Physics.Raycast(transform.position, transform.forward, out H, 150, G_layer))
            Debug.Log(H.transform.name);
            // goes through both walls (layer 8 and 9) and hits target:
            int skip1n2 = ~((1 << 8) | (1 << 19));
            if (Physics.Raycast(transform.position, transform.forward, out H, 150, skip1n2))
            {

            }
                    //Debug.Log(H.transform.name);
            int skip = ~((1 << 8) | (1 << 2) | (1 << 19));
            if (Physics.Raycast(transform.position, transform.forward, out H, 150, skip))
            {

            }*/